📂project_root/
│
├── 📂datasets/                  # Your dataset provided from HuggingFace
│   ├── cartoon/                 # Cartoon images dataset
│   └── realworld/               # Real images dataset
│
├── 📂experiments/               # Folder where model checkpoints/states are saved during training
│
├── 📂logs/                      # Folder used for Tensorboard logs
│
├── 📂outputs/                   # Output images generated by test.py
│
├── 📂models/                    # Necessary model definition now
│   └── StyTR.py                 # Contains TinyViT + MobileNet + Decoder pipeline explicitly clearly
│
├── 📂util/                      # Utilities explicitly clearly used in original implementation
│   ├── __init__.py
│   └── misc.py (needed clearly, others optional)
│
├── 📂function/                  # Loss computation utility functions (needed)
│   ├── normal.py (needed clearly explicitly)
│   ├── calc_mean_std.py (needed clearly explicitly)
│   └── coral.py (optional explicitly)
│
├── sampler.py                   # Sampler explicitly clearly used in training explicitly
├── test.py                      # Testing script explicitly clearly updated version explicitly
├── train.py                     # Training script explicitly clearly updated version explicitly
└── requirements.txt             # Libraries & dependencies (clearly-managed explicitly)